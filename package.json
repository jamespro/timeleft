{
  "name": "timeleft",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "serve -s dist -l 8080",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "typescript": "^5.2.2",
    "vite": "^5.1.0"
  },
  "dependencies": {
    "temporal-polyfill": "^0.2.3"
  }
}
